import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  nome: {
    fontSize: 60,
    fontWeight: "bold",
    color: "blue",
  },
  container: {
    backgroundColor: "#000",
    marginBottom: 8,
  },
});
